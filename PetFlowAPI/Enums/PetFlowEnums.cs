namespace PetFlowAPI.Enums
{
    public enum CouponStatus
    {
        DISPONIVEL,
        RESGATADO,
        EXPIRADO
    }

    public enum HealthEventStatus
    {
        PENDENTE,
        REALIZADO,
        CANCELADO
    }

    public enum SubscriptionStatus
    {
        ATIVO,
        ENCERRADO,
        CANCELADO,
        EXPIRADO
    }
}
